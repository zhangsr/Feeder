<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorAccent">@color/main_grey_dark</item>-->
        <!--<item name="colorControlNormal">@color/main_grey_dark</item>-->
        <!--<item name="colorControlActivated">@color/main_grey_dark</item>-->
        <!--<item name="colorControlHighlight">@color/main_grey_dark</item>-->
    </style>

    <style name="AddSourceTheme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="searchViewStyle">@style/SearchView</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="SearchView" parent="Widget.AppCompat.SearchView">
        <item name="searchHintIcon">@null</item>
        <item name="queryBackground">@null</item>
        <item name="submitBackground">@null</item>
    </style>
</resources>
